<Window x:Class="app_wpf.CrearHabitacion"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:app_wpf"
        mc:Ignorable="d"
        Title="CrearHabitacion" Height="450" Width="800">
    <Window.DataContext>
        <local:CrearHabitacionVM></local:CrearHabitacionVM>
    </Window.DataContext>
    <StackPanel>
        <Border HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock FontSize="20">CREAR HABITACION</TextBlock>
        </Border>
        <Grid Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Número de Habitación -->
            <WrapPanel Grid.Row="0" Margin="0,10,0,10">
                <TextBlock Text="Nº" Margin="0,0,5,0"/>
                <TextBox Text="{Binding Numero, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}">
                    <TextBox.ToolTip>
                        <Binding Path="(Validation.Errors)[0].ErrorContent" RelativeSource="{RelativeSource Self}"/>
                    </TextBox.ToolTip>
                </TextBox>
            </WrapPanel>

            <!-- Tipo de Habitación -->
            <WrapPanel Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center">
                <TextBlock Text="Tipo de Habitación:" Grid.Row="0" Margin="0,0,7,0"/>
                <ComboBox x:Name="TipoHabitacionesComboBox" SelectedItem="{Binding TipoHabitacion, UpdateSourceTrigger=PropertyChanged}"/>
            </WrapPanel>

            <!-- Capacidad -->
            <WrapPanel Grid.Column="2" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Capacidad de" Margin="0,0,5,0"/>
                <TextBlock Margin="0,0,5,0">adultos</TextBlock>
                <TextBox Margin="0,0,5,0" Text="{Binding CapacidadAdultos, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}">
                    <TextBox.ToolTip>
                        <Binding Path="(Validation.Errors)[0].ErrorContent" RelativeSource="{RelativeSource Self}"/>
                    </TextBox.ToolTip>
                </TextBox>
                <TextBlock Margin="0,0,5,0">y niños</TextBlock>
                <TextBox Text="{Binding CapacidadNinos, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}">
                    <TextBox.ToolTip>
                        <Binding Path="(Validation.Errors)[0].ErrorContent" RelativeSource="{RelativeSource Self}"/>
                    </TextBox.ToolTip>
                </TextBox>
            </WrapPanel>

            <!-- Descripción -->
            <StackPanel Grid.Row="1" Margin="0,0,10,0">
                <TextBlock Text="Descripción:"/>
                <TextBox Height="150" Text="{Binding Descripcion, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}">
                    <TextBox.ToolTip>
                        <Binding Path="(Validation.Errors)[0].ErrorContent" RelativeSource="{RelativeSource Self}"/>
                    </TextBox.ToolTip>
                </TextBox>
            </StackPanel>

            <!-- Precio -->
            <WrapPanel Grid.Row="2" Margin="0,10,0,10" HorizontalAlignment="Left" VerticalAlignment="Center">
                <TextBlock Text="Precio:" Margin="0,0,5,0"/>
                <TextBox Text="{Binding Precio, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}">
                    <TextBox.ToolTip>
                        <Binding Path="(Validation.Errors)[0].ErrorContent" RelativeSource="{RelativeSource Self}"/>
                    </TextBox.ToolTip>
                </TextBox>
                <TextBlock>€</TextBlock>
            </WrapPanel>

            <!-- Piso -->
            <WrapPanel Grid.Row="2" Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center">
                <TextBlock Text="Piso:" Margin="0,0,5,0"/>
                <TextBox Text="{Binding Piso, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}">
                    <TextBox.ToolTip>
                        <Binding Path="(Validation.Errors)[0].ErrorContent" RelativeSource="{RelativeSource Self}"/>
                    </TextBox.ToolTip>
                </TextBox>
            </WrapPanel>

            <!-- Fotos -->
            <StackPanel  Grid.Column="1" Grid.Row="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition></ColumnDefinition>
                        <ColumnDefinition></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Fotos:" />
                    <Button Grid.Column="1" Content="Seleccionar"/>
                </Grid>
                <Image Height="150"></Image>
            </StackPanel>

            <!-- Camas -->
            <StackPanel Grid.Column="2" Grid.Row="1" Margin="10,0,0,0">
                <TextBlock Text="Camas:"/>
                <Grid Margin="0,0,0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition></ColumnDefinition>
                        <ColumnDefinition Width="0.2*"></ColumnDefinition>
                        <ColumnDefinition></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <Image Width="70" Height="70" Margin="0,0,7,0" Source="/image/camaDobleIcon.png"></Image>
                    <TextBlock Margin="0,0,7,0" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="22"
                    Text="{Binding CamasDobles, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}">
                        <TextBlock.ToolTip>
                            <Binding Path="(Validation.Errors)[0].ErrorContent" RelativeSource="{RelativeSource Self}"/>
                        </TextBlock.ToolTip>
                    </TextBlock>
                    <WrapPanel Grid.Column="2" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Button Content="+" Margin="0,0,7,0" Width="60"></Button>
                        <Button Content="-" Width="60"></Button>
                    </WrapPanel>
                </Grid>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition></ColumnDefinition>
                        <ColumnDefinition Width="0.2*"></ColumnDefinition>
                        <ColumnDefinition></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <Image Width="70" Height="70" Margin="0,0,7,0" Source="/image/camaIndividualIcon.png"></Image>
                    <TextBlock Margin="0,0,7,0" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="22"
                               Text="{Binding CamasIndividuales, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}">
                        <TextBlock.ToolTip>
                            <Binding Path="(Validation.Errors)[0].ErrorContent" RelativeSource="{RelativeSource Self}"/>
                        </TextBlock.ToolTip>
                    </TextBlock>
                    <WrapPanel Grid.Column="2" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Button Content="+" Margin="0,0,7,0" Width="60"></Button>
                        <Button Content="-" Width="60"></Button>
                    </WrapPanel>
                </Grid>
            </StackPanel>

            <!-- Dimensiones -->
            <WrapPanel Grid.Column="2" Grid.Row="2" HorizontalAlignment="Left" VerticalAlignment="Center">
                <TextBlock Text="Dimensiones (m²):" Margin="0,0,5,0"/>
                <TextBox Text="{Binding Dimensiones, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}">
                    <TextBox.ToolTip>
                        <Binding Path="(Validation.Errors)[0].ErrorContent" RelativeSource="{RelativeSource Self}"/>
                    </TextBox.ToolTip>
                </TextBox>
            </WrapPanel>
        </Grid>
        <!-- Botón Guardar -->
        <Button Grid.Row="10" Content="Guardar" Margin="30,10,30,0"/>
    </StackPanel>
</Window>
